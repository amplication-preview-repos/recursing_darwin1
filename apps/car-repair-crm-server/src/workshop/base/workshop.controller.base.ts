/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { WorkshopService } from "../workshop.service";
import { WorkshopCreateInput } from "./WorkshopCreateInput";
import { Workshop } from "./Workshop";
import { WorkshopFindManyArgs } from "./WorkshopFindManyArgs";
import { WorkshopWhereUniqueInput } from "./WorkshopWhereUniqueInput";
import { WorkshopUpdateInput } from "./WorkshopUpdateInput";
import { AppointmentFindManyArgs } from "../../appointment/base/AppointmentFindManyArgs";
import { Appointment } from "../../appointment/base/Appointment";
import { AppointmentWhereUniqueInput } from "../../appointment/base/AppointmentWhereUniqueInput";

export class WorkshopControllerBase {
  constructor(protected readonly service: WorkshopService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Workshop })
  async createWorkshop(
    @common.Body() data: WorkshopCreateInput
  ): Promise<Workshop> {
    return await this.service.createWorkshop({
      data: data,
      select: {
        address: true,
        createdAt: true,
        id: true,
        name: true,
        phone: true,
        updatedAt: true,
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [Workshop] })
  @ApiNestedQuery(WorkshopFindManyArgs)
  async workshops(@common.Req() request: Request): Promise<Workshop[]> {
    const args = plainToClass(WorkshopFindManyArgs, request.query);
    return this.service.workshops({
      ...args,
      select: {
        address: true,
        createdAt: true,
        id: true,
        name: true,
        phone: true,
        updatedAt: true,
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Workshop })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async workshop(
    @common.Param() params: WorkshopWhereUniqueInput
  ): Promise<Workshop | null> {
    const result = await this.service.workshop({
      where: params,
      select: {
        address: true,
        createdAt: true,
        id: true,
        name: true,
        phone: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Workshop })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async updateWorkshop(
    @common.Param() params: WorkshopWhereUniqueInput,
    @common.Body() data: WorkshopUpdateInput
  ): Promise<Workshop | null> {
    try {
      return await this.service.updateWorkshop({
        where: params,
        data: data,
        select: {
          address: true,
          createdAt: true,
          id: true,
          name: true,
          phone: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Workshop })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async deleteWorkshop(
    @common.Param() params: WorkshopWhereUniqueInput
  ): Promise<Workshop | null> {
    try {
      return await this.service.deleteWorkshop({
        where: params,
        select: {
          address: true,
          createdAt: true,
          id: true,
          name: true,
          phone: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Get("/:id/appointments")
  @ApiNestedQuery(AppointmentFindManyArgs)
  async findAppointments(
    @common.Req() request: Request,
    @common.Param() params: WorkshopWhereUniqueInput
  ): Promise<Appointment[]> {
    const query = plainToClass(AppointmentFindManyArgs, request.query);
    const results = await this.service.findAppointments(params.id, {
      ...query,
      select: {
        agent: {
          select: {
            id: true,
          },
        },

        client: {
          select: {
            id: true,
          },
        },

        createdAt: true,
        dateAndTime: true,
        id: true,
        status: true,
        updatedAt: true,

        workshop: {
          select: {
            id: true,
          },
        },
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/appointments")
  async connectAppointments(
    @common.Param() params: WorkshopWhereUniqueInput,
    @common.Body() body: AppointmentWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      appointments: {
        connect: body,
      },
    };
    await this.service.updateWorkshop({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/appointments")
  async updateAppointments(
    @common.Param() params: WorkshopWhereUniqueInput,
    @common.Body() body: AppointmentWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      appointments: {
        set: body,
      },
    };
    await this.service.updateWorkshop({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/appointments")
  async disconnectAppointments(
    @common.Param() params: WorkshopWhereUniqueInput,
    @common.Body() body: AppointmentWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      appointments: {
        disconnect: body,
      },
    };
    await this.service.updateWorkshop({
      where: params,
      data,
      select: { id: true },
    });
  }
}
